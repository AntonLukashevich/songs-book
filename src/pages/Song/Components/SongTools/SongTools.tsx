import React from "react";
import {Button} from "@mui/material";

export const SongTools = () => {
  let state = {
    isOpen: false
  }

  return(
    <div>
      <Button>Chords</Button>
    </div>
  )
}